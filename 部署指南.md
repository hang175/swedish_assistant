# 部署指南：Supabase（账号和进度同步）+ Vercel（网站）

全程只需要复制粘贴和点按钮，不用改任何代码或数据文件。大约 20–30 分钟。

应用是"本地优先"的：进度永远先存在浏览器里，不登录也能用；登录后自动同步到云端，电脑和手机的进度按"每个词取最近一次作答"合并。

---

## 第 1 步：Supabase（存账号和进度）

1. 打开 https://supabase.com ，注册并 **New project**。名字随意，Region 选 **Central EU (Frankfurt)** 或 **North EU (Stockholm)**，数据库密码让它自动生成并保存好（平时用不到）。
2. 项目建好后，左侧 **SQL Editor → New query**，把项目里 `supabase/schema.sql` 的全部内容粘贴进去，点 **Run**。看到 "Success" 即可。这一步建了一张 `progress` 表，并设置了"每个人只能读写自己那一行"的权限。
3. 左侧 **Project Settings → API**（或 **API Keys**），记下两样东西：
   - **Project URL**（形如 `https://abcdefgh.supabase.co`）https://wprspsqsqwxjxvoowzsx.supabase.co/rest/v1/
   - **anon / publishable key**（一长串字符）   sb_publishable_9YEP2ePTdCRy9O9Lk6Yj3A_TOQ55dgF
   这两个值本来就是公开放在网页里的，不是密码。**不要**用 `service_role` / secret key。
4. （可选，推荐自用阶段这样设）**Authentication → Sign In / Providers → Email**，把 **Confirm email** 关掉。这样注册后立刻能登录，不用等确认邮件（免费版每小时只能发很少的邮件）。以后公开给别人用时再打开。

## 第 2 步：把代码放到 GitHub

1. 注册 https://github.com ，安装 **GitHub Desktop**（https://desktop.github.com ，图形界面，不用命令行）。
2. GitHub Desktop → **File → Add local repository** → 选 `D:\projects\swedish_assistant` → 它会提示这不是仓库，点 **create a repository** → **Create repository**。
3. 点 **Publish repository**，勾选 **Keep this code private**（私有即可，Vercel 一样能部署）。

`.gitignore` 已经配置好：原始数据（Kelly、Folkets、Tatoeba、SAG.pdf）、`node_modules` 和 `.env.local` 都不会被上传；生成好的词库 `public/data/` 会上传。

## 第 3 步：Vercel（网站）

1. 打开 https://vercel.com ，用 GitHub 账号登录 → **Add New… → Project** → 选刚才的仓库 → **Import**。
2. Framework 会自动识别为 **Vite**，不用改。展开 **Environment Variables**，加两条（名字必须完全一致）：

   | Name | Value |
   |---|---|
   | `VITE_SUPABASE_URL` | 第 1 步记下的 Project URL |
   | `VITE_SUPABASE_ANON_KEY` | 第 1 步记下的 anon key |

3. 点 **Deploy**。一两分钟后得到网址，形如 `https://swedish-assistant-xxxx.vercel.app`。

## 第 4 步：告诉 Supabase 你的网址

Supabase → **Authentication → URL Configuration**：
- **Site URL** 填 Vercel 给的网址；
- **Redirect URLs** 里再加一条同样的网址（如果也想在本机用云同步，再加 `http://localhost:5173`）。

这一步是为了让"确认邮件"和"重置密码"邮件里的链接跳回你的网站。

## 第 5 步：开始用

1. 在电脑上打开网址 → **Settings → Account & sync → Create an account**。你本机已有的进度需要先搬过去：在本机版 Settings → **Export backup**，再到线上版 Settings → **Import backup**（本机 `localhost` 和线上网址是两个不同的"浏览器仓库"，不会自动互通）。
2. 手机上用 Safari / Chrome 打开同一个网址，登录同一个账号，进度就过来了。
   - iPhone：Safari 里点 **分享 → 添加到主屏幕**，就像一个 App。
   - 语音：iPhone 和安卓一般自带瑞典语语音；如果没有声音，应用会显示提示。
3. 右上角的小圆点：绿色 = 已同步，黄色 = 同步中，红色 = 同步出问题（进度仍安全地存在本机，稍后自动重试）。

## 以后怎么更新网站

代码有改动后：GitHub Desktop 里写一句说明 → **Commit to main** → **Push origin**。Vercel 会自动重新部署，一两分钟生效。

## 本机版也想用云同步（可选）

把 `.env.example` 复制一份改名为 `.env.local`，填上同样的两个值，重新双击 `start.bat`。——如果不想碰文件，把两个值发给 Claude，让它替你生成这个文件。

## 注意事项

- **免费版 Supabase 项目连续约 7 天没有任何访问会被自动暂停。** 数据不会丢，到 Supabase 后台点 **Restore** 即可。每天背词就不会触发。
- 容量：一个用户的全部进度大约几十到两三百 KB，免费的 500 MB 数据库可以放上千个用户。万一要清理，`supabase/schema.sql` 末尾有两条现成的语句（查看每个用户占用、只保留你自己的数据）。
- 用户可以自己在 Settings 里点 **Delete my cloud data** 删除云端数据。
- 如果以后公开给别人用：打开 Confirm email；在 Supabase 里配置自己的发信服务（SMTP），否则注册邮件会被限流；并准备一页简单的隐私说明（你存了邮箱和学习进度）。
