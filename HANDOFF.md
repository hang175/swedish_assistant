# 瑞典语背词应用：交接文档（HANDOFF）

> 给新会话的说明：这份文档是用户 H 与 Claude 在 chat 模式下讨论确定的方案。方案已获用户确认，**可以直接开始写代码**，不需要重新讨论范围。开工前请先完成"第 0 步"，并就"待确认事项"问用户一句。
>
> 项目文件夹：`D:\projects\swedish_assistant`（用户已把数据文件和一本语法书 PDF 放在里面）

---

## 1. 用户背景与工作方式

- 用户 H 住在瑞典，目前完全不会瑞典语，目标是从零背到 B1/B2 词汇量。和用户用中文交流；应用界面用英语；释义方向为瑞典语→英语。
- 自用工具，**目前主要在电脑（Windows）上背**。手机端（iPhone）是以后的事，现在只需保证窄屏不崩，不做专门适配。
- **用户不手动编辑任何数据文件或代码**（怕改坏）。所有词库数据必须由脚本生成；需要用户做的事情只能是"运行一条命令"或"在界面上点按钮"。
- 用户很看重发音和语法，因为现在一点都不会说。

## 2. 数据来源与许可（已核实，2026-09）

| 来源 | 用途 | 许可 | 义务 |
|---|---|---|---|
| Swedish Kelly list（Språkbanken） | 词表主干：词频、CEFR 等级、词性、en/ett | CC BY-SA 3.0 / LGPL 3.0 双许可 | 署名；衍生数据相同方式共享；官方鼓励引用 Volodina & Johansson Kokkinakis (2012) |
| Folkets lexikon（KTH） | 英文释义、变形、音标、词典自带例句 | CC BY-SA 2.5 | 署名；相同方式共享 |
| Tatoeba（经 manythings.org/anki 整理的版本） | 例句 | CC BY 2.0 FR | 署名：注明 tatoeba.org 与 manythings.org/anki，保留句子 ID 和作者名 |

落到项目里：

1. 应用内要有 **Credits 页面**，列出三个来源、各自许可和链接。
2. 脚本生成的词库文件标为 **CC BY-SA 4.0**（仓库里放 `LICENSE-DATA`）；应用代码单独用 MIT。
3. 只用文本数据，**不使用 Tatoeba 的音频**（音频许可各不相同）。
4. **语法书 PDF 很可能受版权保护**：可以阅读它来参考该覆盖哪些语法主题，但不得把书中内容（原文、例句、表格）搬进应用。语法笔记必须原创撰写。请先问用户这是哪本书。

## 3. 已查看过的数据情况

### Kelly：`Swedish-Kelly_M3_CEFR.xls`
- 旧版 .xls 格式（Node 里用 SheetJS 的 `xlsx` 包可读）。数据在工作表 `Swedish_M3_CEFR`，共 8,425 行；另一张表 `Lookup` 是词性说明。
- 列：`ID`、`Raw freq`、`WPM`（每百万词频）、`CEFR levels`、`Source`、`Grammar`（en / ett / att）、`Swedish items for translation`（词条）、`Word classes`、`Examples`。注意列名里带换行符，读取时按位置取列更稳。
- 等级分布：A1–C1 各 1,404 词，C2 1,405 词。学到 B2 共 5,616 词。
- 词性分布：noun-en 3464、verb 1538、adjective 1354、noun-ett 1065、adverb 569、prep 108、noun 76、pronoun 61、numeral 56，其余为少量功能词类。
- `Source` 取值：SweWaC 约 5,776、T2 2,564、manual 85。
- **坑 1**：最前面 86 行的 WPM 是占位值 1,000,000（主要是人工补录的词，如 brunch、decimeter、syssling、数词、方位词），没有真实词频，却排在 A1 最前。处理：在各自等级内**排到末尾**，让学习从 och、vara、i、ha 开始。T2 来源的词是否有真实 WPM 请检查后同样处理。
- **坑 2**：101 个词条带括号变体，如 `och (vardagl. å; förk. o.)`、`ta (el. taga)`、`kl. (klockan)`、`i (gott) behåll`。处理：清洗出主词形用于匹配和显示，括号内容保留为 `note` 字段。
- **坑 3**：229 个多词词条（短语、小品词动词等）。先尝试整体匹配 Folkets，匹配不上就跳过并记入报告。

### Tatoeba：`swe.txt`（附 `_about.txt`）
- 25,666 行，Tab 分隔三列：英文句子、瑞典语句子、署名串（形如 `CC-BY 2.0 (France) Attribution: tatoeba.org #2877272 (CM) & #8955604 (mr_vile)`）。
- 该版本只收母语者撰写或校对过的句子，质量较好。句长集中在 3–7 个词。
- 解析署名串，保留瑞典语句子的 ID 和作者名。

### Folkets lexikon：**尚未查看**
- chat 会话里没拿到这个文件。请在项目文件夹里找到它（官方文件名通常是 `folkets_sv_en_public.xml`），**先检查实际 XML 结构再写解析代码**。
- 凭印象（需验证）：`<word value="..." class="nn" lang="sv">` 下有 `<translation value="...">`、`<paradigm><inflection value="..."/></paradigm>`、`<phonetic value="...">`、`<example value="..."><translation .../></example>` 等子元素；`class` 为词性缩写（nn、vb、jj、ab、pp、pn……）。需要建立 Kelly 词性 ↔ Folkets 词性的映射表。
- 如果文件夹里没有这个文件，从 https://folkets-lexikon.csc.kth.se/folkets/om.en.html 的下载链接获取瑞→英版本。

## 4. 词库构建脚本

命令：`npm run build:data`。用 TypeScript/Node 写（与应用同一套工具链）。原始数据放 `data/raw/`（不进 git）；生成结果放 `public/data/`（**提交进 git**，这样 Vercel 部署时不需要原始数据）。

规则：

1. 以 Kelly 为主干，按"主词形 + 词性"到 Folkets 找词条。同形异义时用词性消歧。
2. **匹配不上的词直接跳过**，绝不要求用户手动补。
3. 释义：取 Folkets 前 2–3 个译法拼成一条，控制长度，保证选项一行能显示完。
4. 每词保留：词形、词性、en/ett、CEFR、等级内排序、释义、音标（若有）、变形形式（名词：定指单数/复数/定指复数；动词：现在时/过去时/supinum/祈使；形容词：中性/复数/比较级）、备注。
5. 例句：用 Folkets 的变形形式去匹配 Tatoeba 瑞典语句子中的词（整词匹配，不是子串），优先短句，每词最多保留 3–5 条，附英文翻译和署名。Folkets 自带例句也可收入。
6. **干扰项**：每词预生成一个 8–10 个的干扰项池。条件：同词性、同等级或词频排名相近、**释义与正确答案无重叠**（避免近义词导致两个选项都对）、干扰项之间释义也不重复。运行时从池里随机抽 3 个。运行时不调用任何 AI 接口。
7. 输出按 CEFR 等级分成 6 个 JSON 文件，应用按需加载。
8. 同时输出**覆盖率报告**（`build-report.md`）：每级匹配数/跳过数、跳过词清单、有例句的词占比。构建完把关键数字告诉用户。

## 5. 功能范围（第一版）

### 5.1 四选一测验（核心）
- 显示瑞典语单词（名词带 en/ett）；有例句则显示一条瑞典语例句，**英文翻译答完才显示**（否则泄题）。
- 从四个英文释义中选。答完后显示：正误、完整释义、音标、变形形式、多条例句。
- 键盘操作：`1`–`4` 选答案，`空格`/`回车` 下一个，`R` 重听发音。

### 5.2 间隔复习：Leitner 六盒
- 间隔：第 1–6 盒分别为 1 / 2 / 4 / 8 / 16 / 32 天。
- 答对升一盒；答错回第 1 盒，并在**当次学习队列里稍后再次出现**，直到答对。
- 到达第 5 盒算"已掌握"，用于统计。
- "一天"以本地时间凌晨 4:00 为界。

### 5.3 语音朗读
- 用浏览器内置的 Web Speech API（`speechSynthesis`，`sv-SE`），不装包、不花钱、无密钥。
- 单词出现时自动朗读；每条例句旁有播放按钮。设置里可调语速、可关闭自动朗读、可选择声音。
- 启动时检测是否存在瑞典语声音；没有则显示提示：Windows 上推荐用 Edge（自带高质量瑞典语在线语音），Chrome 需在系统设置里安装瑞典语语音包。

### 5.4 每日任务与打卡
- 设置项：每日新词数（默认 20）、每日复习上限（默认 100）、起始 CEFR 等级。
- 按等级内词频从高到低引入新词。
- 首页显示今日进度。**完成当日任务才算打卡**；显示连续打卡天数。
- 统计：按 CEFR 等级显示已掌握词数，如 `A2: 340/1404`（分母用实际匹配成功的词数）。

### 5.5 词典页（Dictionary）
- 浏览完整词表，按等级分组；可搜索（瑞典语或英文）；可按状态筛选（未学 / 学习中 / 已掌握）。
- 点开一个词看：释义、音标、变形、全部例句、发音按钮、当前所在盒子。
- 8,000 行需要用虚拟滚动。

### 5.6 拼写练习（Spelling practice，独立功能）
- **不进入盒子系统，结果不影响复习进度**（用户明确要求：嫌"给英文输入瑞典语"太难，不要混进主流程）。
- 只从已学过的词里抽。听写式：播放发音 + 显示英文释义 → 输入瑞典语单词。
- 提供"提示"按钮逐个揭示字母；输入框旁有 `å` `ä` `ö` 按键；比较时忽略大小写和首尾空格。

### 5.7 数据与备份
- 进度存 localStorage；每词只存盒号、到期日、连对次数等最少字段。
- 设置页提供导出（下载一个 JSON）和导入（校验格式后覆盖，覆盖前二次确认）。

### 5.8 页面
Today（首页）、Study、Dictionary、Spelling、Stats、Settings（含 Credits）。桌面优先布局，窄屏不崩即可。

### 5.9 紧随其后：Grammar 笔记页（核心流程跑通后再做）
- 一个静态页面，15–20 条 A1–A2 语法主题的**原创**简明笔记（en/ett 与定指后缀、复数变化类型、动词时态、V2 语序、否定词位置、疑问句、形容词一致、人称与物主代词、常用介词、小品词动词等），每条配几个例句。
- 内容以静态文件放在仓库里，不影响架构。不得照搬语法书内容。

## 6. 技术约定

- React + Vite + TypeScript，纯前端，无后端、无账号。
- 可部署到 Vercel（静态站点）。
- 依赖尽量少。不引入状态管理库、UI 组件库等重型依赖，除非确有必要。
- SRS 逻辑、每日队列生成、日期界线这几块写成纯函数并配单元测试（Vitest）。

## 7. 明确不做

AI 对话、导入书籍、用户系统、付费、iOS 原生应用、真人音频。**不要提前为这些搭架子。**

## 8. 交付物

1. 能本地运行的完整项目（直接写在项目文件夹里）。
2. 词库构建脚本和覆盖率报告。
3. 简短 README：如何构建词库、如何启动、如何部署到 Vercel、数据来源与许可。
4. `LICENSE`（MIT，代码）和 `LICENSE-DATA`（CC BY-SA 4.0，词库）。

如果当前环境能运行命令，请直接替用户跑 `npm install`、`npm run build:data` 和 `npm run dev` 并验证；如果不能，就给出需要用户执行的确切命令，一次一条。

## 9. 建议的工作顺序

0. **第 0 步**：列出项目文件夹内容，确认三份数据都在；检查 Folkets XML 的真实结构。
1. 写构建脚本 → 跑出词库 → 向用户汇报覆盖率。
2. SRS 与每日队列逻辑 + 单元测试。
3. Study 页（四选一 + 语音 + 键盘操作）。
4. Today、Stats、Settings（含导出/导入、Credits）。
5. Dictionary 页。
6. Spelling 页。
7. README 与许可文件。
8. Grammar 笔记页。

## 10. 待确认事项（开工前问用户）

1. 词典页里要不要加 **"Mark as known"（标为已认识）** 按钮，用来跳过早就会的词？（已提议，用户尚未回答。）
2. 语法书 PDF 是哪一本？（用于判断版权和参考价值；提取困难就先跳过。）
